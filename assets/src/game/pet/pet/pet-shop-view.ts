import { Component } from "cc";
import { View } from "../../../common/base/view";

/** 宠物界面 */
export class PetShopView extends View {
  /** 数据准备 */
  async prepare() {
    return Promise.resolve(1);
  }
}
